# Config baked into the js-dos bundle (docs/city.jsdos).
# js-dos auto-mounts the bundle's files as drive C: and runs [autoexec].
[cpu]
core=auto
cputype=auto
cycles=max

[autoexec]
@echo off
https://github.com/KirinDenis/wire-city-86/blob/main/CITY.COM
exit

TITLE
=====
Lecture 2: The File Is The Program — DOS Game from Scratch (assembly, COM vs EXE)


DESCRIPTION
===========
A COM file has no header. None at all. DOS finds a free segment, copies the
file into it, and jumps to the first byte — so if you open the file in a hex
editor and change five bytes, the program changes. In this lesson we do
exactly that: HELLO becomes WORLD without the assembler running once, and the
source still says HELLO afterwards.

Along the way, the thing nobody shows beginners: what an instruction actually
IS. Eighteen bytes come out of ten lines of source, and every one of those
eighteen can be pointed at and named — this three-byte group is "mov dx, msg",
that two-byte one is "int 21h", and those last six are the letters themselves.

  - writing ten lines of assembly, and what every one of them is for
  - eighteen bytes: the entire program, on one line
  - reading the bytes back against the source, one instruction at a time
  - patching the file with a hex editor, and running it again
  - why org 100h, and what DOS actually does when it loads a COM
  - the same program as an EXE, and what it has to add
  - one line of source decides which kind of file comes out
  - eighteen bytes against eighty-one, and where they went

The assembler is flat assembler, and it is inside the source repository — free
to use and free to pass on. There is no linker: FASM writes the finished file
itself, which is why the choice between a COM and an EXE is a line in the
source rather than a flag on a command line.

  format binary as 'com'      ->  HELLO.COM,  18 bytes
  format MZ                   ->  HELLO2.EXE, 81 bytes

That is the whole fork in the road, and it is in the program, where you can
see it.

DO IT YOURSELF WHILE YOU WATCH — there is a DOS prompt at this address, with
the assembler already sitting on the disk:

https://kirindenis.github.io/wire-city-2/l02.html

Type MAKE and you have built the same three programs I build in the video.
Then HEX HELLO.COM to see the eighteen bytes, and HEX HELLO.COM 12 WORLD to
change them. Nothing to install, nothing to download, and reloading the page
gives you a clean machine — so there is nothing you can break.

Source:
https://github.com/KirinDenis/wire-city-2/tree/main/LESSONS/L02

Tools: DOSBox 0.74-3 and flat assembler, which ships in the repository. The
byte viewer is ours too — HEX.ASM, a hundred and fifty lines, assembled by you
along with everything else. It exists because the file manager I use on camera
is shareware: fine on my machine, not something I can hand out with a lesson.

CHAPTERS
(regenerate after the recording - the timings move:
     Director.exe --chapters L02-thefile)
00:00 What assembly actually is
xx:xx Writing it: ten lines of assembly
xx:xx One line says what kind of file this is
xx:xx Assembling it: no linker, no object file
xx:xx Eighteen bytes, and all of them
xx:xx Reading the bytes back against the source
xx:xx Changing the program without recompiling it
xx:xx Why a COM file works like that
xx:xx The EXE: named segments, an entry point, a stack
xx:xx DS has to be told where the data went
xx:xx Eighteen bytes against eighty-one, and where they went

Previous: Lecture 1 — The Screen Is Just Memory
Next: why the picture tears when it moves, and the second screen that fixes it.

Made by Owlos. We modernise legacy software for the people who still depend on
it, and knowing how it was written the first time is the whole reason we can.
https://owlos.sk

#dos #assembly #retroprogramming #fasm #x86 #retrocomputing #msdos
#reverseengineering #gamedev #dosbox #hexeditor #lowlevel


NOTES FOR UPLOAD
================
- Subtitles: Lecture/voice/L02-thefile.srt, uploaded WITH TIMING.
- Trim the head of the recording to the first spoken word; every timestamp
  above is measured from there.
- Chapters are generated, not typed:
      Director.exe --chapters L02-thefile
- The first three hashtags show above the title. #dos #assembly
  #retroprogramming keep the series consistent; #fasm and #hexeditor are the
  ones that earn their place in this episode.
- The FIRST recording of this lesson used Turbo Assembler and TLINK, and the
  EXE was 562 bytes because TLINK padded it. The numbers spoken on camera must
  match the ones above: 18 and 81.
- PIN THE BROWSER LINK as the top comment as well as putting it in the
  description. It is the only thing in this series a viewer can act on without
  leaving the tab, and it is what turns a lecture into a workshop.
- Volkov on camera is fine and stays: it is legal to USE, only not to ship.
  What the bundle carries instead is HEX.COM. If the two look different on
  screen, say so in one sentence rather than pretending - "the page has a
  smaller tool that does the same two things, and you build it yourself".
